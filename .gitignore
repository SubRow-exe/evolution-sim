# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tests
.pytest_cache/

# 日常のシミュレーション実行データは除外。
# 残す価値のある実験は experiments/ へ厳選してコピーする (seedで完全再現可能)。
runs/

# ベンチマークの一時出力 (benchmarks/ 配下の正式な結果は残す)
bench_local.json

# Secrets / local environment
.env
.env.*
!.env.example

# Private keys / certificates
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519