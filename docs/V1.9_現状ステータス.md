# V1.9 現状ステータス

更新: 2026-09-04
状態: **DESIGN FINAL / IMPLEMENTATION AUTHORIZED / formal experiment未設計**

この文書はV1.9の現在地を一枚で確認するためのステータス正本。
詳細仕様は `docs/V1.9_iLUCA再設計仕様.md`、実装手順は `docs/V1.9_実装チェックリスト.md` を参照する。

---

# 1. V1.8

V1.8は正式終了済み。

- V1.8: scientifically closed
- 旧Exp15: **SUPERSEDED / DO NOT DISPATCH**
- V1.8 parameter tuningへ戻らない
- V1.9実装後に新しいexperimentを設計する

V1.8で得た中心知見:

> day/nightを生存側へ調整する前に、現行iLUCAに内部状態依存homeostasis、進化可能なstorage/reproduction strategy、妥当なchemical-first metabolismが不足していることが判明した。

---

# 2. V1.9目的

> **より妥当なchemical-first iLUCA baselineを作り、自然な環境圧へ生物側が生理・進化で応答できる土台を作る。**

V1.8 day/nightは残すが、初期iLUCAはlight利用を持たない。
phototrophy創発後にday/nightが意味のある周期圧になることを狙う。

---

# 3. P1〜P5 human decisions — CLOSED

## P1/P2 Energy + starvation

確定:

```text
1-pool Energy
storage_capacity gene
storage capacityに維持cost
starvation signal = runway / starvation_horizon
starvation_horizon gene
E_target / reserve 2-poolは廃止
```

homeostasis:

```text
runway不足
 -> BMR可変部/repairを強く抑制
 -> H2/light/nutrient uptakeを弱く抑制
 -> bmr_core/structural upkeepは残す
 -> movementはV1.9では抑制しない
```

## P3 reproduction

確定:

```text
reproduction_horizon gene
Energy gate = runway >= reproduction_horizon
Matter gate = existing repro_matter gate
reproduction_investment / child_matter_fracは維持
```

storage capacity fractionを繁殖gateにしない。

## P4 H2 environment

確定:

```text
H2 explicit / CO2 implicit
4 equal-flux fixed vents
ventはworld端からr以上内側
vent source diskは非重複
H2 diffusion haloあり
H2 environmental lossあり
H2 uptakeで明示的消費
baseline initial spawn = world uniform random
source-biased spawnなし
```

vent間の静的flux差・vent寿命/盛衰はV1.9へ入れない。

## P5 innovation

確定:

```text
continuous mutation と structural innovationを分離
PHOTOTROPHY = innovation-gated
PREDATION   = V1.9 locked
```

初期:

```text
light_absorption = 0
predation_efficiency = 0
PHOTOTROPHY OFF
PREDATION OFF
```

phototrophy innovation後のみlight_absorptionをcontinuous mutationで改良可能。
loss-of-functionも実装する。

---

# 4. V1.9 continuous genes

17 genes:

```text
既存14
+ storage_capacity
+ starvation_horizon
+ reproduction_horizon
```

旧draftの以下は採用しない:

```text
starvation_sensitivity
reserve_capacity
reproduction_threshold
```

---

# 5. V1.9では入れないもの

- Operational/Reserve 2-pool
- E_target controller
- explicit CO2 / carbon cycle
- temperature
- oxygen
- pH
- pressure
- radiation
- toxins
- HGT
- explicit ATP/ADP network
- explicit Wood-Ljungdahl enzyme network
- circadian clock / sunset prediction
- multicellularity
- predation innovation
- vent time variation

これらは必要になった時に独立軸として追加する。

---

# 6. 現在の実装権限

**V1.9本実装を開始してよい。**

Claude / Sonnet等は:

1. `AGENTS.md`
2. `docs/V1.9_iLUCA再設計仕様.md`
3. `docs/V1.9_実装チェックリスト.md`

を読み、コード実査して実装へ進む。

実装中に仕様と現コードが衝突した場合:

- 小さなimplementation detailなら、科学的意味を変えない範囲で実装判断可
- world rule / gene meaning / conservation / innovation semanticsを変える必要がある場合はSTOPして人間へ報告

---

# 7. 実装後の必須確認

- unit/integration tests
- Energy conservation
- Matter conservation
- H2 diffusion conservation
- same-seed determinism
- capability gating
- forced phototrophy innovation sanity
- H2 halo mechanical sanity
- random initial spawn確認

短いmechanical sanityは可。

**生存するようparameterを調整するsanityは不可。**

---

# 8. 現在の禁止事項

実装権限は出たが、以下は禁止。

- 旧Exp15 dispatch
- formal experiment設計/dispatch
- parameter sweep
- 生存目的のenvironment tuning
- innovation probabilityをphototrophyが出るよう自動調整
- V1.8 parameter tuningへ戻る

---

# 9. 次の作業

```text
V1.9 implementation
  ↓
full tests / conservation / determinism
  ↓
mechanical sanity
  ↓
Claude implementation report
  ↓
human review
  ↓
新experiment設計
```

Exp15内容・run数・parameter sweepはまだ決めない。
