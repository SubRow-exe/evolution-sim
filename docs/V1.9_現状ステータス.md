# V1.9 現状ステータス

更新: 2026-09-04
状態: **MECHANISM IMPLEMENTED IN PR #67 / PHYSICAL SCALING DESIGN REOPENED / Exp15 PAUSED**

この文書はV1.9の現在地を一枚で確認するためのステータス正本。

最優先参照:

1. `docs/V1.9_物理スケール再校正方針.md`
2. `docs/V1.9_iLUCA再設計仕様.md`
3. PR #67 `docs/V1.9_実装報告.md`
4. `docs/Exp15_V1.9_実験計画案.md`
5. `docs/次の実験計画.md`

---

# 1. V1.8

V1.8は正式終了済み。

- V1.8: scientifically closed
- 旧V1.8 Exp15: **SUPERSEDED / DO NOT DISPATCH**
- V1.8 parameter tuningへ戻らない

中心知見:

> day/nightを生存側へ調整する前に、iLUCA側にhomeostasis・進化可能なstorage/reproduction strategy・妥当なchemical-first metabolismが不足していた。

---

# 2. V1.9の目的

> **より妥当なchemical-first iLUCA baselineを作り、自然な環境圧へ生物側が生理・進化で応答できる土台を作る。**

初期iLUCAはlight利用を持たず、phototrophy創発後にV1.8 day/nightが意味のある周期圧となることを狙う。

---

# 3. P1〜P5の機構設計 — CLOSED

## P1/P2 Energy + starvation

```text
1-pool Energy
storage_capacity gene
storage capacityにcost
starvation signal = runway / starvation_horizon
E_target / reserve 2-poolは廃止
```

homeostasis:

```text
runway不足
 -> BMR可変部/repairを強く抑制
 -> H2/light/nutrient uptakeを弱く抑制
 -> structural/core maintenanceは残す
 -> movementはV1.9 starvation responseでは直接抑制しない
```

## P3 reproduction

```text
reproduction_horizon gene
Energy gate = runway >= reproduction_horizon
Matter gate = existing repro_matter gate
```

## P4 H2 environment

```text
H2 explicit / CO2 implicit
複数 equal-flux fixed vents
edge clippingなし / source disk非重複
H2 diffusion halo
explicit consumption
baseline initial spawn = uniform random
```

## P5 innovation

```text
continuous mutation と structural innovationを分離
PHOTOTROPHY = innovation-gated
PREDATION   = V1.9 locked
```

---

# 4. PR #67 — mechanism implementation checkpoint

PR #67でV1.9機構実装は完了している。

確認済み:

```text
17 genes
capability gate
1-pool Energy
runway homeostasis
reproduction_horizon gate
H2 explicit field / diffusion halo
uniform random spawn
Recorder
Energy/Matter/H2 conservation
same-seed determinism
274 passed / 5 skipped / 0 failed
CI PASS
```

ただしmechanical sanityで、旧arbitrary-unit referenceのままではfixed iLUCAがvent直上・単独でも約100〜150 tickで餓死することが判明。

これは実装バグではなく、**旧E/tick・substrate/tick・wuの定数間スケールが現実量へ接続されていない問題**と判断した。

したがってPR #67をそのままV1.9 finalとしてmergeしない。

---

# 5. 方針転換 — physical scaling layer

Energy赤字を arbitrary parameter tuning で埋めない。

V1.9の数値スケールを現実単位へ接続する。

確定:

```text
physical time unit = second [s]
standard dt        = 10 s
light cycle        = 24 h
reference day/night= 12 h / 12 h
memory_tau         ≈ 20 s reference
starvation_horizon initial = 1800 s
reproduction_horizon initial = 3600 s
resource-rich iLUCA doubling-time sanity order = 4–8 h, reference ≈6 h
```

H2:

```text
concentration = mol/m^3
amount        = mol
flux          = mol/s
uptake        = mol/s/organism
vent source fluid reference ≈10 mM H2
D_H2 reference ≈5e-9 m^2/s near 25°C
chemical_absorption = physical reference uptakeへのdimensionless multiplier
```

旧 `h2_conversion_eff=0.60` 等をFINAL物理値として使わない。
H2 uptake・free energy・maintenanceを現実オーダーから再定義する。

詳細: `docs/V1.9_物理スケール再校正方針.md`

---

# 6. まだ未決の必須事項

Exp15前に以下を決める。

```text
S1 reference iLUCA cell size / dry mass
S2 world length / grid-cell length / effective depth / voxel volume
S3 Matter <-> dry biomass scale + biomass synthesis energetics
S4 movement speed / power physicalization
S5 dt convergence / invariance criteria
```

これらが無いとH2 specific uptakeを1個体へ変換できず、H2 concentrationからmol amount/source fluxも算出できない。

したがってEnergy収支問題は**見直し方針が確定したが、再校正完了ではない**。

---

# 7. V1.9で引き続き入れないもの

- Operational/Reserve 2-pool
- explicit CO2 / carbon cycle
- temperature
- oxygen
- pH
- pressure
- radiation
- toxins
- HGT
- explicit ATP/ADP network
- explicit Wood-Ljungdahl enzyme network
- circadian clock / sunset prediction
- multicellularity
- predation innovation
- vent time variation

physical unit化はこれら新環境因子を追加することとは別で、既存V1.9基盤の尺度を定義する作業。

---

# 8. 現在AIが実施してよい範囲

現在は**レビュー・設計更新フェーズ**。

可:

- PR #67の実装監査
- physical scaling案のレビュー
- S1〜S5の科学的検討
- docs更新
- scaling用test設計

不可:

- Exp15 formal dispatch
- arbitrary parameter sweep
- fixed iLUCAを生存させるためだけのH2/BMR調整
- PR #67をphysical scaling未反映のままV1.9 finalとして扱う
- phototrophy formal experiment

---

# 9. 次の作業

```text
Opus 5 physical-scaling review
  ↓
S1 reference cell size/dry mass
  ↓
S2 world/grid/voxel physical scale
  ↓
S3 Matter/growth Energy scale
  ↓
S4 movement power scale
  ↓
physical scaling patch on PR #67
  ↓
full tests + conservation + determinism + dt sanity
  ↓
V1.9 final review / merge
  ↓
Exp15 formal details finalize
```

Exp15の科学目的・Phase A/B/C skeletonは維持するが、dispatchはphysical sanity gate完了後。
