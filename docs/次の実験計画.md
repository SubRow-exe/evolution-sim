# 次の実験計画 / 現在の司令塔

更新: 2026-09-04
状態: **V1.8 CLOSED / V1.9 PHYSICAL SCALING REVIEW / Exp15 PAUSED**

> PR #67でV1.9の機構実装は完了したが、旧arbitrary-unit referenceではresource-rich fixed iLUCAにも不可避なEnergy赤字が出た。
> 現在の最優先作業は、V1.9を現実の時間・長さ・質量・H2・Energyスケールへ接続し直すこと。

---

# 1. 最優先参照順

1. `docs/V1.9_現状ステータス.md`
2. `docs/V1.9_物理スケール再校正方針.md` — **現在の設計正本**
3. `docs/V1.9_iLUCA再設計仕様.md` — 機構仕様。旧arbitrary numeric referenceは再校正対象
4. PR #67 `docs/V1.9_実装報告.md` — 機構実装checkpoint / Energy収支ギャップ
5. `docs/Exp15_V1.9_実験計画案.md` — **physical gate後のformal skeleton / DO NOT DISPATCH**
6. `docs/V1.8_総括.md`
7. `docs/環境因子追加・校正方針.md`

旧V1.8 Exp15は **SUPERSEDED / DO NOT DISPATCH**。

---

# 2. 現在地

```text
V1.8                          CLOSED
V1.9 mechanism design          CLOSED
V1.9 mechanism implementation PR #67 / checkpoint complete
V1.9 arbitrary numeric scale   REJECTED AS FINAL
V1.9 physical scaling          CURRENT HUMAN/AI REVIEW TASK
Exp15                          SKELETON ONLY / PAUSED
Exp16                          phototrophy origin candidate / 未設計
```

---

# 3. V1.9で維持する機構

```text
chemical-first iLUCA
light_absorption initial = 0
predation_efficiency initial = 0
Energy = 1-pool
storage_capacity gene + cost
starvation signal = runway / starvation_horizon
reproduction gate = runway >= reproduction_horizon
H2 explicit / CO2 implicit
H2 spatial diffusion halo
uniform random initial spawn
phototrophy = structural innovation gated
predation = locked
```

これらはPR #67で実装済み。

---

# 4. physical scalingで確定済み

```text
time unit = second
standard dt = 10 s
light cycle = 24 h
reference day/night = 12 h / 12 h
memory_tau reference = 20 s
starvation_horizon initial = 1800 s
reproduction_horizon initial = 3600 s
resource-rich generation-time sanity = 4–8 h order (reference ≈6 h)

H2 concentration = mol/m^3
H2 amount        = mol
H2 flux          = mol/s
H2 uptake        = mol/s/organism
vent source-fluid H2 reference ≈10 mM
D_H2 reference ≈5e-9 m^2/s near 25°C
```

旧 `E/tick`、`substrate/tick`、`wu` の数値はphysical scaling完了までFINAL扱いしない。

---

# 5. 現在の必須未決事項

```text
S1 iLUCA reference cell size / dry mass
S2 world length / grid-cell length / effective depth / voxel volume
S3 Matter <-> dry biomass scale / growth energetics
S4 movement speed / power physicalization
S5 dt convergence / invariance criteria
```

これらを決めた後、H2 source mol/s、individual uptake、maintenance、growth、movementを同じSIスケールで閉じる。

---

# 6. Exp15 skeleton

正本: `docs/Exp15_V1.9_実験計画案.md`

```text
Phase 0: physical sanity gate (formal experimentではない)
Phase A: fixed iLUCA baseline
Phase B: storage_capacity + starvation_horizon + reproduction_horizon evolution ON
Phase C: 必要時のみ単独寄与切り分け
```

phototrophy innovationはExp15では科学検証しない。
Exp16候補として分離する。

---

# 7. HARD STOP

現在行わない:

```text
Exp15 formal dispatch
arbitrary parameter sweep
生存させるためだけのH2/BMR tuning
旧tickベースの実験時間窓流用
PR #67をphysical scaling未反映のままV1.9 finalとしてmerge扱い
phototrophy formal experiment
```

---

# 8. 次の順序

```text
1. Opus 5レビュー
2. S1 reference cell size/dry mass
3. S2 world/grid/voxel scale
4. S3 Matter/growth Energy scale
5. S4 movement physical scale
6. physical scaling patch on PR #67
7. tests / conservation / determinism / dt sanity
8. V1.9 final review / merge
9. Exp15 duration / seed /判定基準 final
10. Exp15 dispatch
11. Exp15考察後にExp16 phototrophy origin
```

---

# 9. 恒久判断原則

> **生物を生存させるためにarbitrary環境値を動かすのではなく、可能な限り現実単位・現実オーダーから供給と支出を置き、その環境で自然選択に解かせる。**

fixed ancestorの絶滅だけで環境FAILにしないが、resource-rich条件でも進化可能レバーと無関係に必ず赤字になる場合は、進化圧ではなくscale/model不整合を疑う。
