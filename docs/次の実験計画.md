# 次の実験計画 (2026-09-04 更新)

> 現在の司令塔。**Exp15科学設計は確定。次はSonnetによる実装・preflightであり、formalは人間dispatch待ち。**

---

# 0. 最優先参照順

1. `docs/Exp15_実験計画確定.md` — **Exp15科学条件の正本**
2. `docs/Exp15_実装チェックリスト.md` — **Sonnet実装仕様の正本**
3. `docs/Exp14_表現型プロベナンス訂正.md` — **Exp14実行表現型の正式訂正**
4. `docs/環境因子追加・校正方針.md` — プロジェクト全体の恒久校正方針
5. `docs/V1.8_現状総括_Exp14結果.md` — Exp13/14結果・背景
6. `docs/V1.8_一次Energy生態非対称仕様.md` — V1.8物理仕様
7. `docs/Exp14_実験計画確定.md` — Exp14事前登録履歴。**再dispatchしない**
8. `docs/Exp14_レビュー判断.md`
9. `docs/Exp13_結果考察_中間.md`
10. `docs/V1.8_Exp13_レビュー判断.md`
11. `docs/Exp13_実験計画確定.md` — **再dispatchしない**
12. `docs/V1.7_総括.md`
13. `docs/メインストリーム開発ストーリー.md`
14. `docs/数値再現性・Actions実行環境方針.md`
15. `docs/実験結果保存方針.md`
16. `docs/バージョニング方針.md`

---

# 1. 現在地

```text
V1.7 / Exp11 / Exp12          完了 / bmr_core=.15
V1.8実装                      完了
Exp13                         完了 / light calibration scientific STOP
Exp13 chemical grid           完了 / 暫定候補あり
Exp14                         完了 / 116/116 raw回収
Exp14 phenotype provenance    訂正済み / actual light_absorption=.3
Exp15科学設計                 確定
Exp15実装                     Sonnet実装待ち
Exp15 preflight/formal        未実行
V1.8 light working reference  未選定
V1.8 final acceptance         未完了
```

Exp13/Exp14をそのまま再実行しない。

---

# 2. 全体の校正方針

`docs/環境因子追加・校正方針.md`を恒久方針とする。

今後、気温・重力・水深/水圧・酸素・pH・放射線・毒物など多数の環境因子を追加し、iLUCA（LUCA-inspired）から陸上移動・飛翔まで進化可能な世界を目指す。

したがって:

```text
原因を切り分ける
-> 粗い成立域を見つける
-> 複数世代成立 / 意味ある選択圧 / 非支配的 / knife-edgeでないことを確認
-> working referenceを暫定採用
-> 次の機構へ進む
-> 多因子化後に全体balanceを再調整
```

唯一の厳密最適値・正確な相転移点は通常追わない。

---

# 3. Exp14の正式な扱い

Exp14計画ではExp13 A1と同じlight specialist:

```text
light_absorption=2.0
chemical_absorption=.3
```

を意図した。

しかし実際の`make_exp14_configs.py`には`diagnostic_gene_overrides`が無く、formal workflowは同generatorを直接使ったため、116/116 runは実際には:

```text
INITIAL_GENOME
light_absorption=.3
chemical_absorption=.3
```

で開始した。

詳細: `docs/Exp14_表現型プロベナンス訂正.md`

維持する実測事実:

```text
116/116 run 最終絶滅
死亡原因 = starvation
Generation 2 = 0
```

同一Exp14内の相対傾向はactual phenotype=.3条件での探索的証拠として残す。
ただしExp13/V1.7 light specialistとの定量比較や恒久parameter選定には使わない。

---

# 4. Exp15の目的

V1.8で同時導入した:

```text
1. day/night cycle
2. primary Energy density response
```

を**正しい固定light specialistで独立に切り分ける。**

その後、combined worldで`light_max`だけを粗く振り、working reference候補を探す。

---

# 5. Exp15 Phase A

共通:

```text
light specialist: light_absorption=2.0 / chemical_absorption=.3
全14遺伝子固定
light-only
light_max=1.2
seed=1..5
ticks=5,000
```

条件:

```text
A0 cycle OFF / density OFF
A1 cycle OFF / density ON
A2 cycle ON  / density OFF
A3 cycle ON  / density ON
A4 A0完全duplicate sentinel
```

A0-A3はfeature flag以外を変えない。
A0/A4は同seedで科学出力一致を要求する。

Phase A = 25 formal runs。

A0 baselineが成立しなければ`BASELINE_NOT_VIABLE`として科学STOPし、Phase Bを自動実行しない。

---

# 6. Exp15 Phase B

Phase A後に**別dispatch**。

combined固定:

```text
cycle ON
density ON
light_uptake_half=.6
period=200
day_fraction=.5
energy_capacity=100
```

唯一のsweep:

```text
light_max = 1.2 / 2.4 / 4.0 / 6.0 / 8.0 / 12.0
seed = 1..3
ticks = 10,000
```

Phase B = 18 formal runs。

working referenceは、隣接するtested light levelが2つ連続で3/3 seed viableになった最初のpairの低い側を候補とする。

成立しなければ`NO_ROBUST_LIGHT_REFERENCE`で終了し、Exp15内へperiod/capacity/K sweepを後付けしない。

---

# 7. 最重要technical gate

Exp14再発防止:

Configだけでなく`Simulation(cfg, seed)`を実際に初期化し、初期100個体全てについて:

```text
light_absorption == 2.0
chemical_absorption == 0.3
```

を独立testする。

これが通らない限りformal禁止。

また観測追加はRNG/state/update orderへ非干渉であることをtestする。

---

# 8. 中心観測量

```text
realized light gain / organism-tick
maintenance+movement / organism-tick
repair / organism-tick
birth overhead / organism-tick
core net Energy / organism-tick
Energy/capacity distribution
light H(I,K) operating distribution
birth timing
population
survival/extinction
max generation
sunset/dawn reserve
```

nightのI=0はH分布へ混ぜない。

---

# 9. Actions運用

`exp15.yml`は:

```text
mode=preflight
mode=phase_a
mode=phase_b
```

を構造的に分離する。

- preflight成功からformalへ自動遷移しない
- Phase A成功からPhase Bへ自動遷移しない
- formal前にphase全体wall-clock予測
- technical FAILとscientific STOPを分離
- artifact完全性を機械確認

詳細は`docs/Exp15_実装チェックリスト.md`。

---

# 10. Chemical

Exp13暫定候補:

```text
chemical_uptake_half=1.5
chem_uptake=.5
```

Exp14 light-only結果はこのcandidateを否定しない。
Exp15 light整理後に長期validationへ戻る。

---

# 11. V1.8をfinalizeしない

未完了:

```text
Exp15
light working reference
chemical long-term validation
light-only long-term viability
chemical-only long-term viability
mixed evolution validation
V1.8 ACCEPT
v1.8-final保存
```

V1.8導入済み機構のworking parameter調整はV1.8 finalizationであり、V1.9へ前倒ししない。

---

# 12. 次の実作業

Sonnetへ:

> `AGENTS.md` → `docs/Exp15_実験計画確定.md` → `docs/Exp15_実装チェックリスト.md` → `docs/Exp14_表現型プロベナンス訂正.md` の順に読み、Exp15 harnessを実装する。全testsと実装監査まで行う。preflight/formalは勝手にdispatchしない。
