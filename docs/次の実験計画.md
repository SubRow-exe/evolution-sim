# 次の実験計画 / 現在の司令塔

更新: 2026-09-04
状態: **V1.9 PHYSICAL PATCH IMPLEMENTATION / Exp15 PHASE 0 NEXT**

> 議論フェーズは一旦終了。現在の最優先は `docs/V1.9_検証実装仕様_物理スケール版.md` をPR #67 branchへ実装し、Exp15 Phase 0で仮説を実測すること。

---

# 1. 最優先参照順

1. `docs/V1.9_現状ステータス.md`
2. `docs/V1.9_検証実装仕様_物理スケール版.md` — **実装正本**
3. `docs/Exp15_V1.9_実験計画案.md` — **Phase 0 / formal A-B計画**
4. `docs/V1.9_iLUCA再設計仕様.md`
5. PR #67 `docs/V1.9_実装報告.md`
6. `docs/V1.9_物理スケール再校正方針.md`
7. Issue #68 Opus 5 review

旧V1.8 Exp15は **SUPERSEDED / DO NOT DISPATCH**。

---

# 2. 現在地

```text
V1.8                          CLOSED
V1.9 mechanism design          CLOSED
PR #67 mechanism implementation DONE / checkpoint
V1.9 physical verification spec FINAL FOR TEST
V1.9 physical patch            CURRENT TASK
Exp15 Phase 0                  NEXT
Exp15 Phase A/B                READY IN DESIGN / BLOCKED UNTIL P0 PASS
Exp16 phototrophy              FUTURE
```

---

# 3. 今回の検証baseline

```text
1 agent = 1 cell
reference dry mass = 0.28 pgDW
1 matter unit = 0.28 pgDW
time = seconds
biology dt = 10 s
world = 20 mm x 20 mm
40x40 / dx=0.5 mm / depth=0.5 mm
4 H2 point-source cells / resolved source 1 mM
D_H2 = 5e-9 m2/s
exchange tau = 900 s
automatic diffusion subcycling
H2 uptake = physical Michaelis-Menten
Energy = J / W
usable H2 Energy = 3750 J/mol
basal maintenance = physical ATP-maintenance anchor
binary fission reference = 50:50
Exp15 damage axes = OFF
```

H2 depletion competitionはExp15成功条件にしない。
主要圧はH2-rich / H2-poor空間によるnet-Energy heterogeneity。

---

# 4. 実行順

```text
1. PR #67 branchへphysical-scale patch
2. unit/integration tests
3. Energy/Matter/H2 conservation
4. same-seed determinism
5. Phase 0 harness
6. P0-A no-organism H2 field
7. P0-B radial single-cell balance
8. P0-C random exposure
9. P0-D dt=2.5/5/10 s convergence
10. implementation/preflight report
11. STOPして人間レビュー
```

Phase 0 PASS後:

```text
Phase A fixed iLUCA: 5 seeds x 10 days
 -> adequacy review
Phase B 3-gene evolution: same 5 seeds x 10 days
 -> A/B analysis
Phase C: human review後のみ
```

---

# 5. Phase 0 HARD GATE

最重要条件:

> world内に `net power > 0` と `net power < 0` の距離帯が両方存在すること。

```text
全域positive -> 選択圧不足 / STOP
全域negative -> scale or model mismatch / STOP
```

FAIL時の自動parameter tuningは禁止。

---

# 6. 現在AIが実行してよい範囲

- V1.9 physical patch実装
- tests / ledger / determinism
- Phase 0 harness実装
- Phase 0実行
- report作成

formal Exp15 A/BはPhase 0 PASS前にdispatchしない。

---

# 7. 恒久原則

> **議論だけで最適値を探し続けず、現実オーダーから置いた検証可能なbaselineを実装し、preflightで仮説の当否を測る。**

PASSさせるためのarbitrary tuningはしない。
