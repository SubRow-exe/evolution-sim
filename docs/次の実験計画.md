# 次の実験計画 (2026-09-03 更新)

> 現在の司令塔。**V1.7の科学検討は終了。次はV1.7正式close → V1.8実装 → Exp13。**

---

# 0. 最優先参照順

1. `docs/V1.8_Exp13_レビュー判断.md` — **Opus 5レビューの人間採否・最優先**
2. `docs/V1.8_一次Energy生態非対称仕様.md` — **V1.8実装正本**
3. `docs/Exp13_実験計画確定.md` — **Exp13条件・選定規則・run構成正本**
4. `docs/V1.8_実装チェックリスト.md` — **実装漏れ防止HARD GATE**
5. `docs/V1.7_総括.md` — **V1.7閉幕・Exp11/12総括**
6. `docs/Exp12_結果考察.md`
7. `docs/メインストリーム開発ストーリー.md`
8. `docs/数値再現性・Actions実行環境方針.md`
9. `docs/LUCA参照モデル方針.md`
10. `docs/実験結果保存方針.md`
11. `docs/バージョニング方針.md`

Opus 5レビュー原文はbranch `claude/review-v1-7-exp11-kflr82` commit `6dba4b4`。**レビュー原文の提案より1〜4を優先する。**

---

# 1. 現在地

```text
V1.4 / Exp08                      完了
V1.5 / Exp09                      完了
V1.6 / Exp10                      完了
V1.7 bmr_core実装                 完了
Exp11                            255 run完了 / 再集計・考察済み
Exp12                            71/71正式run完了 / 考察済み
V1.7科学判断                      bmr_core=0.15で終了
V1.7 default反映 / v1.7-final     ← Sonnet 5最初の作業
V1.8実装                          ← その次
Exp13                            ← V1.8実装・監査後
```

Exp13結果考察は次の**V1.8チャット**で行う。

---

# 2. V1.7最終判断

恒久値:

```text
bmr_core = 0.15
```

Exp12 B1:

```text
0.00〜0.10 -> lower-bound equilibrium
0.15       -> interior equilibrium 8/8
0.20       -> interior 7/8 + converging 1
0.30       -> interior 6/8 + converging 2
```

B1 `DELAY_CONTINUES=0`。

Exp12の事前登録機械判定はB2 method-control未達により:

```text
SCIENTIFIC_VERDICT = INVALID_OR_METHOD_REVIEW
```

だったことを正式記録する。

人間判断ではB2を:

```text
METHOD CONTROL ASSUMPTION INVALID
```

すなわち「B2自身が50kでも変化しており、既知stationary controlという前提が不成立」と扱う。

B2科学runを失敗扱いせず、V1.7目的のための追加100kは行わない。

詳細: `docs/V1.7_総括.md`。

---

# 3. V1.8のテーマ

```text
light
= broad / renewable / day-night / low-average / intensity dependent

chemical
= localized stock / depletable / competitive / high-local-return potential
```

実装:
- half-sine day/night、**energy中立正規化しない**
- `H(x,K)=x/(x+K)`をlight/chemical直接吸収へ
- fair sharing維持
- chemical stock depletion維持
- actual light supply ledgerへ修正

禁止:
- light直接penalty
- chemical固定bonus
- light user movement強制OFF
- plant/cyanobacteria/chloroplast class
- finite vent lifetime
- INITIAL_GENOME.light_absorption=0（V1.9事項）

---

# 4. V1.7 closeを先に行う

Sonnet 5はV1.8変更前に:

```text
Config.bmr_core default 0.0 -> 0.15
```

を反映。

その後:
1. full pytest
2. conservation
3. determinism
4. CI Green
5. V1.7 CI基準ref更新
6. `v1.7-final`保存

まで完了する。

V1.7 closeとV1.8変更を同一の曖昧なcommitへ混ぜない。

---

# 5. Exp13概要

## Phase 0

軽量HARD GATE:
- Config/manifest
- V1.8 unit/integration
- Energy/Matter
- 2 feature flagの4組合せ
- current runner 2k×2 determinism
- collector E2E
- CI
- runtime preflight

## Phase A1 — light map

```text
light_max = 0.8,1.2,1.5,1.8,2.1,2.4,3.0,4.0
5 seed ×10k = 40 run
```

全水準を実行し、絶滅域〜豊富域を保存。

working light:
> `ROBUST_LIGHT_VIABLE`を満たす最小値

初期個体を各光量へ合わせて救済しない。

## Phase A2 — chemical 2D map

```text
chemical_uptake_half = 0.5,1.5,3.0,6.15
chem_uptake          = 0.5,1.0,2.0,4.0
16 combo ×3 seed ×5k = 48 run
```

realized H、stock、depletion、populationを地図化。

selected pairは:
1. 成立する最小chem_uptake
2. その中でrealized median Hが0.5に最も近いK

## A2b/A3

```text
selected vent/random validation = 10 run
density competition             = 9 run
```

Phase A total = **107 run**。

## Phase B

```text
B1 light-only long-term        8 ×20k
B2 chemical-only long-term     8 ×20k
B3 mixed 2-gene evolution     12 ×30k
B4a body_size=.246 fixed       3 ×5k
B4b body_size-only evolution   5 ×20k
```

Phase B = **36 run**。

Formal simulation planned total:

```text
107 + 36 = 143 run
```

B3/B4の解釈はExp13完了後のV1.8チャットで行う。

---

# 6. BURST_RATIO方針

旧`BURST_RATIO >= X`は選定HARD GATEから削除。

目的はchemicalをlightの人工的な倍数にすることではない。

重視:
- 生存
- 継続繁殖
- population
- Energy収支
- stock depletion/recovery
- vent occupancy
- density competition
- mixed worldでの実利用

同一初期窓のgain ratioや理論最大取得量は診断値として残してよい。

---

# 7. 実行時間 — 今回の重要追加要求

**Exp13 Phase A正式dispatch前に、Sonnet 5はExp13全体のwall-clock概算をユーザーへ報告する。**

最低限:
- 143 formal run予定
- phase別run/tick
- representative benchmark
- max-parallel
- single-run最大概算
- waveを考慮したphase wall-clock
- Phase 0 / selection / collectを含む**全体合計予測時間**
- safety factor / uncertainty

ざっくりでよいが、1 run時間だけでは不可。

新しい時間計測処理は追加しない。

実験後はprediction vs actualを結果へ記録。

---

# 8. 実装ミス再発防止

Exp11/12で起きた:
- fixed_genes漏れ/誤名
- snapshot形式誤認
- artifact大量取得欠落
- historical runner bit一致の誤HARD GATE
- checker/testが同じ誤定数を共有
- single-runと全体時間の混同

を再発させない。

`docs/V1.8_実装チェックリスト.md`全項目とrequirement traceabilityがformal前HARD GATE。

AIは途中デバッグのためCI/pushを乱発しない。ローカルで論理単位を完成させてからpush/CI。

---

# 9. Sonnet 5への作業順

```text
1. 最新main / AGENTS / 本司令塔を読む
2. V1.7総括・レビュー判断を読む
3. bmr_core=.15 default反映
4. tests/CI/ref更新/v1.7-final
5. V1.8実装
6. Exp13 generator/checker/summarizer/workflow実装
7. requirement traceability完成
8. local full tests
9. CI
10. Phase 0
11. runtime benchmark
12. **Exp13全体の予測終了時間をユーザーへ報告**
13. 全HARD GATE PASSならformal Exp13実行
14. collect/integrity
15. 結果・runtime実績保存
```

科学候補・閾値をSonnet判断で変更しない。

---

# 10. V1.9以降

mainstream:

```text
V1.8  source物理分化
  ↓
V1.9  chemical-first ancestorからphototrophy創発
  ↓
dynamic vents / resource turnover
  ↓
HGT
  ↓
engulfment / intracellular symbiosis
  ↓
plastid-like integration
  ↓
oxygenic photosynthesis / planetary feedback
```

V1.9前に`light_absorption=0`から有意値までの期待待ち時間を軽量検算する。

engulfment以降はデータモデル変更規模が大きいことを前提に別設計する。

詳細: `docs/メインストリーム開発ストーリー.md`。
