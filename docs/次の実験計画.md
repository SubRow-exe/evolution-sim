# 次の実験計画 (2026-08-27 更新)

本書は「今どこまで分かっていて、次に何をすべきか」を一箇所にまとめた作業計画である。
着手する人・AIは**まず本書を読み、終わったら結果と本書の状態表を更新すること。**

- 長期の方向性: [開発ロードマップ_リアル化方針.md](開発ロードマップ_リアル化方針.md)
- 観測性・性能の方針: [改善方針_Ver1.2_観測性と性能.md](改善方針_Ver1.2_観測性と性能.md)
- 実験結果の実物: `experiments/`

---

## 1. 現在地

Ver.1.1 は `v1.1-baseline` タグで凍結済み。ロードマップ Stage 0 は完了している。

### 分かったこと

| 事実 | 根拠 |
|---|---|
| 適応度関数なしで方向性選択が発生する | exp01 (光利用能力 0.30→0.445) |
| 個体急増は**5 seed中2 seedでのみ起こる断続的転移** | exp02 |
| 転移の実体は**単一系統による選択的一掃** (最大系統シェア 3.7%→96.2%) | exp02 `lineages.csv` |
| mutation_rate上昇は**ヒッチハイク**であり直接選択ではない | exp02 (非転移seedでは上昇しない) |
| 転移は繁殖条件の抜け道ではなく**ニッチ開拓**の側面が強い | exp02 (光利用率 23%→37%) |
| 物質は厳密に保存されている | 全run + `tests/test_conservation.py` |

### 未解決の問い

1. **転移はどのくらいの確率・時期で起こるのか** (5 seedでは統計不足)
2. **転移の必要条件は何か** (どの遺伝子の可変性が引き金か)
3. **転移後どうなるのか** (高mutation_rate 0.203 は維持されるか、低下に転じるか)
4. **body_size下限0.2のhard clampが結果を決めていないか** (seed 4で0.344まで接近)

---

## 2. 次の実験 (優先度順)

### Exp 03: 転移の発生率と時期の分布 ★最優先

**問い**: 転移は何%のseedで、いつ起こるのか。

```bash
uv run python tools/run_batch.py --seeds 1-20 --ticks 40000 --workers 5
```

- 40,000 tickまで延ばす (exp02では30,000で未転移のseedが残った)
- **注意**: 転移seedは個体数が4倍になり実行時間が約2.6倍になる。
  20 seedで数時間規模。`--workers` はCPUコア数に合わせる

**記録すること**: 転移した seed の割合、転移tickのヒストグラム、
転移seedと非転移seedの最終遺伝子分布の比較。

**判定**: 転移tickが特定の範囲に集中するなら、蓄積された変異が閾値を超える
決定論的な過程の可能性が高い。広く散らばるなら偶然性が支配的。

---

### Exp 04: アブレーション実験 (改善方針 §3)

**問い**: 転移に必要な遺伝子の可変性はどれか。

**転移が確実に起きるseed (exp02より seed 1 と 4)** を使い、
一度に1遺伝子だけ変異を止めて転移が消えるかを見る。

固定する候補:

- `body_size`
- `reproduction_investment`
- `mutation_rate`
- `light_absorption`

**実装済み** — `--fix-genes` で指定した遺伝子は変異せず初期値のまま固定される。

```bash
uv run python main.py --headless --ticks 40000 --seed 4 --fix-genes body_size
uv run python main.py --headless --ticks 40000 --seed 4 --fix-genes mutation_rate
uv run python main.py --headless --ticks 40000 --seed 4 --fix-genes light_absorption
uv run python main.py --headless --ticks 40000 --seed 4 --fix-genes reproduction_investment
```

固定遺伝子は**初期ばらつきも与えられない**ため、全個体・全世代で完全に一定となる。
また乱数は固定遺伝子の分も消費するので、他の遺伝子の変異系列は通常実行と一致する
(「その遺伝子だけが違う」比較が成立する)。

**注意**: 一度に複数条件を変えないこと。まず seed 4 (転移が最も明確) で試す。

---

### Exp 05: 転移後の長期挙動

**問い**: ヒッチハイクで上がった mutation_rate (0.203) はその後どうなるか。

```bash
uv run python main.py --headless --ticks 150000 --seed 4
```

高い突然変異率は有害変異も増やすため、一掃が終わって集団が均一化した後は
不利に転じて低下する可能性がある。これが観察できれば
**「ヒッチハイクで上昇 → 選択で低下」という二段階の現象**を捉えたことになる。

**注意**: 個体数3,600規模で15万tickは長時間。`max_population_halt` (既定20,000) に
達した場合は自動保存して停止する。Ctrl+Cでも途中結果は完全に出力される。

---

### Exp 06: 災害の影響 (仕様書 Experiment 4)

**問い**: 転移直前・転移直後に大量絶滅が起きると何が変わるか。

一掃が進行中の集団に災害を与えると、遺伝的浮動で転移が潰れる可能性がある。

**実装済み** — `--disaster-at` で指定tickに災害を起こせる (カンマ区切りで複数可)。

```bash
uv run python main.py --headless --ticks 40000 --seed 4 --disaster-at 25000
uv run python main.py --headless --ticks 40000 --seed 4 --disaster-at 20000,25000,30000
```

seed 4 の転移は約25,400 tickなので、その直前・最中・直後で結果がどう変わるかを比較する。

---

## 3. 実装タスク

| # | 内容 | 規模 | 用途 | 状態 |
|---|---|---|---|---|
| T1 | `--fix-genes` — 指定遺伝子の変異を停止 | 小 | Exp 04 | **実装済み** |
| T2 | `--disaster-at N` — 指定tickで災害 | 小 | Exp 06 | **実装済み** |
| T3 | 系統樹の可視化 (`events.csv` から再構築) | 中 | 一掃した系統の起源特定 | 未着手 |
| T4 | 区間別プロファイリング (改善方針 §1後半) | 中 | 長時間実験の高速化 | 未着手 |

T1・T2はいずれも既定では無効であり、`v1.1-baseline` の挙動を変えない。

### 設計上の宿題 (実験より先に決めない)

- **hard clamp の見直し** (改善方針 §8): `body_size` 下限0.2が転移の到達点を
  決めている疑いがある。ただし**先に Exp 03/04 で転移の性質を把握してから**着手する。
  制約を変えると baseline との比較ができなくなるため
- 観測情報と世界の真の状態の分離 (改善方針 §6) → ロードマップ Ver.1.2 Stage 1

---

## 4. 進め方のルール

1. **1実験につき1条件のみ変更する。** 複数変えると原因が特定できない
2. 結果は `experiments/expNN_<名前>/` に `NOTES.md` 付きで保存する
   (`events.csv` と `snapshots/` は容量次第で除外可。seedがあれば再現できる)
3. **baseline (`v1.1-baseline`) の挙動を変える変更は、実験が一段落するまで入れない**
4. 変更を入れたら `uv run pytest tests` を全通しする (CIでも自動実行)
5. 本書の「現在地」と「次の実験」を更新してからPRを出す
